using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MovePieces : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //ik wil dat de pieces bewegen op het bord. dit moet horizontaal of verticaal gebeuren.

        //hoe wil ik dit doen? 
        //als je klikt op 1 piece, wordt deze gehighlight. daarna klik je op de tile waar je hem naartoe wilt verplaatsen. Als je op een tile klikt waar je de piece niet naar kan verplaatsen, 
        //krijg je een foutmelding. Voor nu is de max 2, later hangt dit af van het aantal stappen dat je nog over hebt.
    }

    private void OnMouseUp()
    {
        
    }


}
